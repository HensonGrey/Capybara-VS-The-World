import { View } from "react-native";
import React from "react";

const Player = () => {
  return (
    <View
      style={{
        position: "absolute",
        left: 50,
        bottom: 20,
        width: 50,
        height: 50,
        backgroundColor: "red",
      }}
    />
  );
};

export default Player;
